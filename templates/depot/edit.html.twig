{% extends 'base.html.twig' %}

{% block title %}Editer Depot{% endblock %}

{% block body %}


 <div class="container">
        <h2 class="text-center mt-5 mb-3">Editet Depot</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_depot_index') }}"> 
                    Voir tout les Depots
                </a>
            </div>
            <div class="card-body">
                {% include 'depot/_form.html.twig' with {'button_label': 'MAJ Depot', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>

{% endblock %}
