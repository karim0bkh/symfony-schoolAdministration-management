{% extends 'base.html.twig' %}

{% block title %}Nouveau User{% endblock %}

{% block body %}

    <div class="container">
        <h2 class="text-center mt-5 mb-3">Créer User</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_user_index') }}"> 
                    Voir tout les Users
                </a>
            </div>
            <div class="card-body">
                 {{ include('user/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
